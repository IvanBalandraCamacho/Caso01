# ============================================================================
# SINGLE LLM - GPT-4o-mini Configuration
# ============================================================================

# Proveedor LLM único: OpenAI GPT-4o-mini
LLM_PROVIDER=openai

# --- Google Gemini ---
# DEPRECATED: Ya no se usa, solo para referencia
# GEMINI_API_KEY=your-gemini-api-key-here
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_PRO_MODEL=gemini-1.5-pro

# Configuración adicional de LLM
ACTIVE_LLM_SERVICE=GEMINI

# ============================================================================
# SINGLE LLM SYSTEM
# ============================================================================

# Sistema Single-LLM activado (siempre usar GPT-4o-mini)
MULTI_LLM_ENABLED=false

# OpenAI GPT-4o-mini (Modelo único)
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini

# ============================================================================
# Consultar con el equipo que desarrolla el servicio RAG
RAG_SERVICE_API_KEY=

# Timeout en segundos para requests al servicio RAG
RAG_SERVICE_TIMEOUT=30

# Activar servicio RAG (ahora habilitado por defecto)
RAG_SERVICE_ENABLED=true

# ============================================================================
# SEGURIDAD Y AUTENTICACIÓN
# ============================================================================

# JWT Secret Key para firmar tokens
# CRÍTICO: En producción, genera una clave fuerte con:
#   openssl rand -hex 32
# o
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-use-openssl-rand-hex-32

# Algoritmo de cifrado JWT
JWT_ALGORITHM=HS256

# Tiempo de expiración del token de acceso (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
#
# 1. SINGLE-LLM:
#    - El sistema usa únicamente GPT-4o-mini de OpenAI
#    - Todas las tareas (análisis, chat, generación) usan el mismo modelo
#    - Mayor simplicidad y consistencia
#
# 2. API KEYS:
#    - OpenAI: De pago, obtener en platform.openai.com
#    - El JWT en token.txt se usa como OPENAI_API_KEY si no se configura aquí
#
# 3. ARQUITECTURA RAG:
#    - RAG_SERVICE_ENABLED=false: Usa Qdrant local (desarrollo/transición)
#    - RAG_SERVICE_ENABLED=true: Usa servicio RAG externo (producción)
#    - Redis se usa para cache y cola de Celery
#    - MySQL guarda metadata, conversaciones y mensajes
#
# 4. SEGURIDAD:
#    - NUNCA expongas tus API keys en código o repos públicos
#    - Usa .env solo en desarrollo local
#    - En producción, usa secrets managers (AWS Secrets, Azure Key Vault, etc.)
#    - Genera JWT_SECRET_KEY único por ambiente
#
# ============================================================================

